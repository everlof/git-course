#!/bin/bash

git init

# Skapa att-handla.txt

git add att-handla.txt
git commit -m "Lade till 'att-handla.txt'"

# Skapa att-göra.txt

git add att-göra.txt
git commit -m "Lade till 'att-göra.txt'"

# Redigera att-handla.txt

git add att-handla.txt
git commit -m "Köpte bröd - lade till Ost till handlelista"

